# requirements.txt

# libraries to install with pip for the ECHO project
pymavlink
pyserial
matplotlib
numpy
PyQt6

# install by running: pip install -r requirements.txt 

# standard library (no install needed): tkinter, struct, time, threading, csv, os, datetime, queue, collections