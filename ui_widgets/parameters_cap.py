#paramters_cap.py

# send capture ON or OFF to ARES params

cap_params = [
    'AVS_SEND_CAP_OFF',
    'AVS_SEND_CAP_ON'
]

cap_params_description =[
    '(send capture OFF command to ARES, default: 0 = disabled)',
    '(send capture ON command to ARES, default: 0 = disabled)'
]

cap_params_values = [
    0,
    0
]

